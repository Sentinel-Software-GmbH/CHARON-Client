{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: clang.exe build active file",
            "command": "ceedling.cmd",
            "args": [
                "build",
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}